Добавить скил-кнопку в чате (как выключатель, можно подключать по необходимости, по умолчанию выключена). Которая будет давать возможность модели заглянуть во всю базу данных @alias.
    - Сюда же проверить возможность добавить эмбединг модель которая бы работала над @alias, если это может улучшить работу с данными.
        - Если есть такая возможность, векторизировать сразу всю базу с @alias, а не отдельные ее части.

    - Давай я опишу сценарий использования @alias в обычном режиме. Обычно я задействую 1 или 2 @alias из всего списка. Что может составлять 1% или даже 0.1%. И вот вопрос, логично ли в таком случае векторизировать всю базу целеком. Сможем ли мы потом передавать в нейросеть только отдельные части. Выбирая нужное из списка @alias.

---

Необходимо добавить какую-то легкую возможность бекапа сразу всех БД в один архив. Например с помощью ZIP или 7z, не важно. Чтобы они складывались в отдельную директорию из которой я мог бы сохранить этот архив в отдельные места, чтобы в случае чего не утратить всю занесенную в базу информацию.
    - Замечу, что лучше всего для начала сделать ручной пуск через скрипт. Но в дальнейшем это будет автоматизировано скорее всего.

---

Мне нужна кнопка внутри галереи которая с подтверждением будет отчищать все изображения из галереи. 

---

На превью изображений в чате должны быть такие же всплывающие кнопки как на превью в галерее. Скрепка, Загрузить, Удалить (должно удалять из базы/локальный файл и из галереи тоже, т.е. все и насовсем)

---

Необходимо добавить Drag'n'Drop для документов и изобажений (желательно с визуальным подтверждение). А так же вставку из буфера обмена для документов и изображений.
